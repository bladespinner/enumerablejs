define(["../notImplementedFunction"],
function(UnimplementedFunction){
  function IEnumerable(){
    this.getIterator = UnimplementedFunction;
    this.enumerate = UnimplementedFunction;
    this.count = UnimplementedFunction;
    this.where = UnimplementedFunction;
    this.take = UnimplementedFunction;
    this.skip = UnimplementedFunction;
    this.select = UnimplementedFunction;
    this.orderBy = UnimplementedFunction;
    this.aggregate = UnimplementedFunction;
  }
  return IEnumerable;
});